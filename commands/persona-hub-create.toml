description = "Create or enrich a persona from folders, files, URLs, or text"
prompt = "Follow the /persona-hub-create skill instructions. Guide the user through creating or enriching a persona. Accept any source type: folders of transcripts, individual files, URLs, or text descriptions. If the persona already exists, enrich it with new sources. {{args}}"
